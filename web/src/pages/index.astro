---
import {Card, CardGrid} from '@astrojs/starlight/components'

import WithSidebar from '~/layouts/WithSidebar.astro'
import {SOCIALS, USER_NAME, NICK_NAME} from "../consts";
---

<WithSidebar>
    <Fragment slot="sidebar">
        <aside class="bg-white border-r border-slate-200 p-6
        flex flex-col gap-y-2 text-sm text-slate-800">

            <div class="flex flex-col items-center">
                <img
                        class="size-36 rounded-full border-4 border-white shadow-md"
                        src={`https://github.com/${USER_NAME}.png`}
                        alt={USER_NAME}
                />
                <h1 class="mt-3 text-xl font-bold">{NICK_NAME}</h1>
                <p class="text-slate-500">@{USER_NAME}</p>
                <p class="text-center text-slate-600">{"Overflow Stack Developer."}</p>
            </div>


            <div class="flex justify-around text-center">
                <div>
                    <span class="font-bold block">{106}</span>
                    <span class="text-xs text-slate-500">followers</span>
                </div>
                <div>
                    <span class="font-bold block">{98}</span>
                    <span class="text-xs text-slate-500">following</span>
                </div>
            </div>

            <div class="flex flex-col text-center py-4">
                {
                    SOCIALS.map((social) => (
                            <a class="flex no-underline h-6 items-center" href={social.url} target="_blank">
                                <span class="sr-only">{social.description}</span>
                                <img class="scale-50" src={social.icon} alt={social.description}/>
                                <p class="text-xs text-slate-500">@{social.name}</p>
                            </a>
                    ))
                }
            </div>
        </aside>
    </Fragment>
    <h1 class="flex-1 p-6">👋 Hi there</h1>
    <h3>Welcome to my website, here I will:</h3>

    <div class="size-12 bg-red-500"></div>

    <CardGrid stagger={true}>
        <Card title="Post my posts." icon="rocket">
        </Card>
        <Card title="Share my thoughts." icon="star">
        </Card>
        <Card title="Show my projects." icon="puzzle">
        </Card>
        <Card title="Show what I'm working on." icon="approve-check-circle">
        </Card>
    </CardGrid>

</WithSidebar>
