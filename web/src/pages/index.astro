---
import WithSidebar from '~/layouts/WithSidebar.astro'
import ProjectCard from '../components/ProjectCard.astro'
import FeaturedProjectCard from '../components/FeaturedProjectCard.astro'
import ProfileSidebar from '../components/ProfileSidebar.astro'
import { fetchGitHubProfile, fetchGitHubRepos, fetchFeaturedProjects } from '../lib/github'
import { PINNED_PROJECTS } from '../consts'

const profile = await fetchGitHubProfile()
const repos = await fetchGitHubRepos()
const featured = await fetchFeaturedProjects(PINNED_PROJECTS)
---

<WithSidebar>
    <Fragment slot="sidebar">
        <ProfileSidebar profile={profile} showBio />
    </Fragment>

    <h1 class="page-title anim-fade" style="--delay:0">👋 恁好，吃了冇</h1>

    <p class="page-subtitle anim-fade" style="--delay:1">可求欢迎恁来俺嘞网站，还能搁这儿：</p>

    <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-12 anim-fade" style="--delay:2">
        <a href="/blog" class="group flex items-center gap-2 rounded-xl neu-btn
                                px-4 py-3 no-underline text-[var(--text-primary)] transition-all duration-300
                                hover:bg-gradient-to-r hover:from-[var(--nav-blog-hover-from)] hover:to-transparent">
            <span class="text-base">🚀</span>
            <span class="text-xs font-semibold group-hover:text-[var(--nav-blog-group-text)] truncate">怼点文章</span>
            <span class="ml-auto text-[var(--nav-blog-text)] text-xs group-hover:text-[var(--nav-blog-text-hover)] transition-colors">›</span>
        </a>
        <div class="flex items-center gap-2 rounded-xl neu-inset opacity-50
                    px-4 py-3 text-[var(--text-muted)] cursor-default">
            <span class="text-base grayscale-[30%]">⭐</span>
            <span class="text-xs font-medium truncate">怼点想法</span>
            <span class="ml-auto text-[9px] text-[var(--text-muted)] whitespace-nowrap">soon</span>
        </div>
        <a href="/projects" class="group flex items-center gap-2 rounded-xl neu-btn
                    px-4 py-3 no-underline text-[var(--text-primary)] transition-all duration-300
                    hover:bg-gradient-to-r hover:from-[var(--nav-proj-hover-from)] hover:to-transparent">
            <span class="text-base">🧩</span>
            <span class="text-xs font-semibold group-hover:text-[var(--nav-proj-group-text)] truncate">怼点项目</span>
            <span class="ml-auto text-[var(--nav-proj-text)] text-xs group-hover:text-[var(--nav-proj-text-hover)] transition-colors">›</span>
        </a>
        <div class="flex items-center gap-2 rounded-xl neu-inset opacity-50
                    px-4 py-3 text-[var(--text-muted)] cursor-default">
            <span class="text-base grayscale-[30%]">✅</span>
            <span class="text-xs font-medium truncate">怼点想怼嘞</span>
            <span class="ml-auto text-[9px] text-[var(--text-muted)] whitespace-nowrap">soon</span>
        </div>
    </div>

    <section class="board-container neu-board anim-fade" style="--delay:3">
        <h2 class="neu-section-title mb-6">
            📌 置顶项目
            <span class="neu-section-divider"></span>
        </h2>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-12">
            {featured.map((project) => (
                <FeaturedProjectCard
                    name={project.name}
                    description={project.description}
                    language={project.language}
                    stars={project.stargazers_count}
                    url={project.html_url}
                    homepage={project.homepage}
                    bannerUrl={project.bannerUrl}
                    logoUrl={project.logoUrl}
                    owner={project.owner}
                    badges={project.badges}
                />
            ))}
        </div>

        <h2 class="neu-section-title mb-6">
            开源项目
            <a href="/projects" class="text-sm font-normal text-[var(--text-muted)] hover:text-[var(--text-link)] no-underline transition-colors">瞅瞅 →</a>
            <span class="neu-section-divider"></span>
        </h2>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
            {repos.map((repo) => (
                <ProjectCard
                    name={repo.name}
                    description={repo.description}
                    language={repo.language}
                    stars={repo.stargazers_count}
                    url={repo.html_url}
                />
            ))}
        </div>

        <div class="mt-6 text-center">
            <a href="/projects" class="inline-flex items-center gap-1.5 text-sm text-[var(--text-muted)] hover:text-[var(--text-link)] no-underline transition-colors">
                瞅瞅全部项目 →
            </a>
        </div>
    </section>
</WithSidebar>
