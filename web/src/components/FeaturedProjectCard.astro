---
import { getImage } from 'astro:assets'
import { getLanguageColor } from '../lib/language-colors'

interface Props {
  name: string
  description: string | null
  language: string | null
  stars: number
  url: string
  homepage: string | null
  bannerUrl: string | null
  logoUrl: string | null
  owner: string
}

const { name, description, language, stars, url, homepage, bannerUrl, logoUrl, owner } = Astro.props
const languageColor = getLanguageColor(language)
const hasBanner = !!bannerUrl
const hasLogo = !!logoUrl

// Generate a subtle hue for no-banner fallback
const hue = name.split('').reduce((acc, c) => acc + c.charCodeAt(0), 0) % 360

// Optimize remote images at build time, output to dist/_astro/ as webp
const bannerSrc = hasBanner
  ? (await getImage({ src: bannerUrl, inferSize: true })).src
  : null
const logoSrc = hasLogo
  ? (await getImage({ src: logoUrl, inferSize: true })).src
  : null
const ownerAvatarSrc = (await getImage({
  src: `https://avatars.githubusercontent.com/${owner}`,
  width: 28,
  height: 28,
})).src

// Pre-compute gradient for no-banner fallback
const fallbackGradient = `linear-gradient(135deg, hsl(${hue}, 60%, 95%) 0%, hsl(${hue + 30}, 50%, 90%) 50%, hsl(${hue + 60}, 40%, 92%) 100%)`
---

<a
  href={url}
  target="_blank"
  rel="noopener noreferrer"
  class="featured-card group block relative rounded-2xl overflow-hidden
         bg-white backdrop-blur-sm
         border border-slate-200 shadow-sm
         transition-all duration-500 ease-out no-underline
         hover:shadow-[0_20px_60px_-15px_rgba(0,0,0,0.15)]
         hover:-translate-y-2 hover:border-slate-300"
  style={`--card-accent: ${languageColor}; --card-hue: ${hue}`}
>
  {/* Hover glow border effect */}
  <div class="absolute -inset-px rounded-2xl opacity-0 group-hover:opacity-100
              transition-opacity duration-500 -z-10 blur-sm"
       style={`background: linear-gradient(135deg, ${languageColor}66, ${languageColor}22, ${languageColor}66)`} />

  {/* Banner / Visual area */}
  <div class="relative h-40 overflow-hidden">
    {bannerSrc ? (
      <img
        src={bannerSrc}
        alt=""
        class="absolute inset-0 w-full h-full object-cover
               transition-transform duration-700 ease-out
               group-hover:scale-110"
        loading="lazy"
      />
    ) : (
      <div class="absolute inset-0 overflow-hidden"
           style={`background: ${fallbackGradient}`}>
        {/* Geometric decoration */}
        <div class="absolute inset-0">
          <div class="absolute top-4 right-4 size-24 rounded-full opacity-20"
               style={`background: ${languageColor}`} />
          <div class="absolute -bottom-6 -left-6 size-32 rounded-full opacity-10"
               style={`background: ${languageColor}`} />
          <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2">
            <span class="text-6xl font-black select-none opacity-[0.08] tracking-wider"
                  style={`color: ${languageColor}`}>
              {name.toUpperCase()}
            </span>
          </div>
        </div>
      </div>
    )}

    {/* Bottom gradient fade */}
    <div class="absolute inset-x-0 bottom-0 h-12 bg-gradient-to-t from-white/40 to-transparent" />

    {/* Stars badge */}
    {stars > 0 && (
      <div class="absolute top-3 right-3 z-10">
        <span class="inline-flex items-center gap-1 px-2.5 py-1 rounded-full
                      bg-white/90 backdrop-blur-md text-slate-700 text-xs font-semibold
                      shadow-sm border border-white/50">
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16" fill="currentColor"
               class="size-3 text-amber-500">
            <path d="M8 .25a.75.75 0 0 1 .673.418l1.882 3.815 4.21.612a.75.75 0 0 1 .416 1.279l-3.046 2.97.719 4.192a.75.75 0 0 1-1.088.791L8 12.347l-3.766 1.98a.75.75 0 0 1-1.088-.79l.72-4.194L.818 6.374a.75.75 0 0 1 .416-1.28l4.21-.611L7.327.668A.75.75 0 0 1 8 .25z" />
          </svg>
          {stars}
        </span>
      </div>
    )}
  </div>

  {/* Content area */}
  <div class="relative px-5 pb-5 -mt-3">
    {/* Logo + Title row */}
    <div class="flex items-start gap-3.5">
      {/* Logo */}
      {logoSrc && (
        <div class="shrink-0 -mt-8 relative z-10">
          <div class="size-14 rounded-xl overflow-hidden bg-white
                       shadow-lg ring-[3px] ring-white
                       flex items-center justify-center">
            <img
              src={logoSrc}
              alt={`${name} logo`}
              class="size-full object-contain p-1.5"
              loading="lazy"
            />
          </div>
        </div>
      )}

      {/* Title + Owner */}
      <div class:list={["min-w-0 flex-1", { "pt-2": !hasLogo }]}>
        <h3 class="featured-title m-0 font-bold truncate text-slate-800
                    group-hover:text-slate-900 transition-colors duration-300">
          {name}
        </h3>
        <div class="flex items-center gap-1.5 mt-0.5">
          <img
            src={ownerAvatarSrc}
            alt=""
            class="size-3.5 rounded-full opacity-60"
            loading="lazy"
          />
          <span class="text-[11px] text-slate-400 font-medium">{owner}</span>
        </div>
      </div>
    </div>

    {/* Description */}
    <p class="mt-2.5 mb-0 text-[13px] text-slate-500 line-clamp-2 leading-relaxed">
      {description ?? 'No description provided'}
    </p>

    {/* Footer: language + homepage */}
    <div class="mt-3 pt-3 border-t border-slate-100/80 flex items-center justify-between gap-2">
      {language ? (
        <span class="inline-flex items-center gap-1.5 text-[11px] font-medium text-slate-500">
          <span class="size-2.5 rounded-full shrink-0" style={`background-color: ${languageColor}`} />
          {language}
        </span>
      ) : <span />}

      {homepage && (
        <span
          class="inline-flex items-center gap-1.5 text-[11px] font-bold tracking-wide uppercase
                 rounded-md px-3 py-1.5 cursor-pointer
                 text-white shadow-sm
                 hover:shadow-md hover:brightness-110
                 transition-all duration-200 active:scale-95"
          style={`background: ${languageColor}; box-shadow: 0 2px 8px ${languageColor}44`}
          onclick={`event.preventDefault(); event.stopPropagation(); window.open('${homepage}', '_blank')`}
        >
          Homepage
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16" fill="currentColor"
               class="size-3 transition-transform duration-200 group-hover:translate-x-0.5">
            <path fill-rule="evenodd" d="M4.22 11.78a.75.75 0 0 1 0-1.06L9.44 5.5H5.75a.75.75 0 0 1 0-1.5h5.5a.75.75 0 0 1 .75.75v5.5a.75.75 0 0 1-1.5 0V6.56l-5.22 5.22a.75.75 0 0 1-1.06 0Z" clip-rule="evenodd" />
          </svg>
        </span>
      )}
    </div>
  </div>
</a>

<style>
  .featured-card {
    will-change: transform, box-shadow;
    color: rgb(30 41 59) !important;
  }
  .featured-card:hover {
    color: rgb(30 41 59) !important;
  }
  .featured-title {
    font-size: 1.5rem !important;
    line-height: 2rem !important;
  }
</style>
